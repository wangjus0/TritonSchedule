{
  "private": true,
  "license": "MIT",
  "scripts": {
    "setup": "bash scripts/setup.sh",
    "setdown": "bash scripts/setdown.sh",
    "teardown": "npm run setdown",
    "dev": "concurrently -n client,server -c blue,grey \"npm --prefix frontend run dev\" \"npm --prefix backend run build && npm --prefix backend run start:server\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run build && npm --prefix backend run start:server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
